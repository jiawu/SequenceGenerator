#ifndef REPORTERSEQUENCE_H
#define REPORTERSEQUENCE_H

#include <iostream>
#include <string>

//ReporterSequence should have the following methods:
//generate_likely_sequence, input: PWM, width. output: ReporterSequence Object
//add_random_spacer, generates a random spacer between 1-5 nucleotides long
//add_polya_tail, which adds a tail

class ReporterSequence: public BasicSequence
{
  private:
    ReporterSequence() { }
  public:
    ReporterSequence(int max_size, string sequence_name) //default constructor
    //the default constructor should 
    void

class 

#endif
